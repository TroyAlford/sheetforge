import { Playground } from 'docz'
import { types } from 'mobx-state-tree'
import Attribute from '@/models/Attribute'
import Character from '@/models/Character'
import Effect from '@/models/Effect'
import EffectList from './EffectList'

## EffectList Component

<Playground>
  {() => {
    const character = Character.create({
      attributes: [
        { displayName: 'Strength', id: 'STR', value: 16 },
        { displayName: 'Dexterity', id: 'DEX', value: 14 },
        { displayName: 'Charisma', id: 'CHA', value: 9 },
        { displayName: 'Attack', id: 'ATK', value: 3 },
      ],
      traits: [{
        displayName: 'Trait',
        effects: [
          { modifier: 4, modifies: 'STR' },
          { modifier: 4, modifies: 'DEX' },
          { modifier: 4, modifies: 'CHA' },
          { condition: 'vs Goblins', modifier: 1, modifies: 'ATK' },
        ],
      }]
    })

    return <EffectList expanded model={character.traits.at(0).effects} />
  }}
</Playground>
